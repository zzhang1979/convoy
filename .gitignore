# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Runtime data (SQLite lives next to the server by default)
server/convoy.db
*.db
*.db-wal
*.db-shm
# Local agent credentials (Sprint 6 test run) — never commit secrets
.secrets/
*.secret
register_jasmine.py
